{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import openpyxl\" 2>/dev/null || pip3 install openpyxl -q 2>/dev/null || pip install openpyxl -q 2>/dev/null || echo '[ai-labor-risk] WARNING: Could not install openpyxl. Run: pip install openpyxl'"
          }
        ]
      }
    ]
  }
}
